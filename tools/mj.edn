#mj {:project #project {:profile :jar}
     :targets
     #merge
     [#default-targets [:pom :clean :jar :uberjar :compile :binary :install :deploy]
      {:test {:doc     "Run tests"
              :invoker :clojure
              :main    kaocha.runner
              :aliases [:dev :test]
              :options {:out             :inherit
                        :forward-options false}}}]}
